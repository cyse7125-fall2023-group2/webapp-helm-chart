apiVersion: v1
kind: Service
metadata:
  labels:
    app: "webapp-service"
  name: "webapp-service"
spec:
  selector:
    app: "webapp"
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 4000
  type: LoadBalancer